# Core JAX training stack.
jax[cuda12]==0.10.0
flax==0.12.7
optax==0.2.8
numpy>=2.0
PyYAML>=6.0
jaxtyping>=0.3
wandb>=0.26

# Data preparation / inspection.
pyarrow>=24.0
requests>=2.32
tokenizers>=0.22

# Tests.
pytest>=8.0
